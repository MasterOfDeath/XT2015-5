@{
    Response.Cookies["useridcookie"].Expires = DateTime.Now.AddYears(-1);
    Photos.Logger.Logger.Log.Info($"The user {User.Identity.Name} logged out");
    FormsAuthentication.SignOut();
    Response.Redirect("/");
}